let db = require("../db/tel");

let registerController = {
    login: function(req, res) {
        res.render('index'); // habria que mandarlo a la pagina de login que todavia no esta hecha (cambiar por index)
      }
} 

module.exports = loginController; 